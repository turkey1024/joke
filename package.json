{
  "name": "daily-joke-email",
  "version": "1.0.0",
  "description": "Cloudflare Worker that sends daily jokes via email",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo \"No tests specified\" && exit 0",
    "logs": "wrangler tail"
  },
  "keywords": [
    "cloudflare",
    "worker",
    "email",
    "joke",
    "automation"
  ],
  "author": "sb",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
